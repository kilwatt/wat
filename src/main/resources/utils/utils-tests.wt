import 'std.io'

unit tests {
    fun run(fn) {
        try {
            fn()
            io.println('[92mtest passed.[0m')
        } catch e {
            io.println('[31mtest failed. error: ' + e + '[0m')
        }
    }
}